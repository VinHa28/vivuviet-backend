services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vivuviet-backend
    restart: always
    ports:
      - "5000:5000"
    env_file:
      - .env.production
    environment:
      - NODE_ENV=production
    networks:
      - vivuviet-network

networks:
  vivuviet-network:
    driver: bridge
